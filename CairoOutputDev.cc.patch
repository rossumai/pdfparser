--- poppler/poppler/CairoOutputDev.cc	2017-12-08 13:36:05.848558433 +0100
+++ poppler/poppler/CairoOutputDev.ccx	2017-12-08 13:35:52.092686957 +0100
@@ -416,8 +416,24 @@
   double *dashPattern;
   int dashLength;
   double dashStart;
+  double dashTotal;
+  int i;

   state->getLineDash(&dashPattern, &dashLength, &dashStart);
+  // avoid getting cairo into CAIRO_STATUS_INVALID_DASH error state
+  if (dashLength > 0)
+    dashTotal = 0.0;
+    for (i = 0; i < dashLength; ++i)
+    {
+      if (dashPattern[i] < 0)
+      {
+        dashTotal = 0.0;
+        break;
+      }
+      dashTotal += dashPattern[i];
+    }
+    if (dashTotal == 0.0)
+      dashLength = 0;
   cairo_set_dash (cairo, dashPattern, dashLength, dashStart);
   if (cairo_shape)
     cairo_set_dash (cairo_shape, dashPattern, dashLength, dashStart);
